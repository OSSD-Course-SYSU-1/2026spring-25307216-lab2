# 新增功能描述

## 1. 双击屏幕点赞与连击爆裂特效（强化版）
- **创新点 A：多红心爆裂喷发**：在视频播放区域双击后，不是只弹出单颗爱心，而是会在触点附近瞬间生成一组错位爱心，形成“爆裂喷发”的视觉效果。每颗爱心都带有随机旋转、独立缩放和上浮轨迹，最后逐渐消失；若视频尚未点赞，会自动切换为已点赞并同步更新计数。
- **创新点 B：全屏闪光脉冲**：在多颗爱心出现的同时，再叠加一层短促的全屏白色闪光，让整个点赞反馈更像“高能爆发”，视觉冲击更强，也更容易在展示时吸引注意力。
- **主要交互行为**：
	- 一次双击会生成多颗爱心，形成更明显的连击爆发感。
	- 一次双击会同步触发全屏闪光，让点赞反馈更完整、更有层次。
	- 爱心先快速脉冲放大，再向上漂浮并淡出，单次交互的视觉层次更丰富。
	- 动画结束后自动移除对应实例，避免节点堆积；若视频未点赞，同步调用点赞逻辑并更新 UI。
- **实现要点（开发参考）**：
	- 新增状态 `heartList`（数组）管理每个爱心实例的 `{id, x, y, angle, scale, opacity, offsetY}`，并使用 `heartId` 作为自增唯一 id。
	- 新增状态 `burstFlashOpacity` 和 `burstFlashScale` 用于驱动全屏闪光层的显示与缩放。
	- 使用手势事件的局部坐标（`event.fingerList[0].localX/localY`）作为爱心生成位置，保证视觉与手指位置一致。
	- 双击时通过 `spawnHeartBurst()` 一次性生成 4 颗错位爱心，并同步触发 `BurstFlashLayer()` 的全屏闪光层，利用不同的起始偏移、旋转角度、延迟和明暗变化，让视觉效果更像“高能爆裂”。
	- 生成后通过 `animateTo`（或等价动画 API）驱动放大与上浮/淡出动画；在动画结束后清理数组中的对应项以释放资源。
	- 点赞逻辑复用现有 `toggleLike()`：在生成爱心时判断并调用以同步计数与状态展示（右侧图标与作者栏保持一致）。
- **性能与稳定性**：
	- 快速清理已完成的爱心实例，避免节点累积；通过批量生成、闪光层短时播放但及时回收的方式，在增强视觉冲击的同时控制性能开销。
	- 动画时长、错位幅度与缓动曲线可提取为常量，便于后续继续强化效果或做设备适配。
- **可选增强**：
- **已实现增强**：
	- 粒子扩散：点赞爆裂同时发射小粒子，增强能量感。
	- 触觉反馈（Haptic）：在支持的设备上触发短时振动以增强反馈感（采用 `vibrator.vibrate()`，有能力时生效）。
	- 音效（留空）：当前仓库未包含音频资源，如需可额外添加并在 `spawnHeartBurst()` 中调用播放逻辑。
- **用户说明**：在视频区域任意位置双击即可触发多颗爱心的爆裂动画并完成点赞（若已点赞仅展示特效）。

## 2. 半模态（抽屉式）面板评论区
- **功能说明**：点击右侧操作栏的“评论（气泡）”图标，会平滑升起一个半屏高度的评论面板。
- **视觉效果**：弹窗内包含顶部评论总数、中间顺滑滚动的模拟用户评论列表，以及最底部的输入框和“发送”按钮。支持手势操作，用户可通过拖动弹窗顶部的横向指示条向下滑动关闭面板。

## 3. 黑胶唱片流光旋转式动效与底部信息栏
- **功能说明**：还原沉浸式短视频应用体验，在界面底部添加核心视觉细节。
- **视觉效果**：右下角增加带有均匀、无限循环旋转动画的“黑胶唱片”音乐封面，提升音乐律动感；左下角补充视频作者的信息（如：@匿名用户）以及多行视频文案描述，高度契合主流短视频App的主界面布局。

## 4. 独立手势高优控制层（防触控冲突逻辑）
- **功能说明**：彻底重构了该页面的点击与手势拦截逻辑，解决了以往“点击操作按钮也会误触视频暂停”的手势冲突问题。
- **实现原理**：剥离了包裹在最外层的全局拦截手势，在底层视频画面与顶层交互UI之间，插入了一个全屏且透明的“专门手势控制层”。这使得当用户点击屏幕中心空白处时，能顺利触发播放、暂停及双击点赞；而点击右侧“评论”、“红心”等功能入口时，系统优先判定为UI交互行为，完美呼出弹窗而绝不打断视频的正常播放。

## 5. 倍速播放功能（新增）
- **功能说明**：在播放界面加入便捷的倍速切换控件，用户可通过界面右上角的倍速按钮在 0.5x / 1.0x / 1.5x / 2.0x 四档之间循环切换，以满足快速观看或慢速复看的场景需求。
- **交互说明**：点击右上角的小按钮即可切换到下一档倍速，当前倍速以标签形式（如 `1.5x`）展示；切换会即时生效并影响当前正在播放的视频。
- **实现要点**：
	- 在 `VideoPlayer` 内新增 `speedIndex` 状态与 `speedValues` 列表，维护倍速档位及对应的 `PlaybackSpeed` 值。
	- 通过更新 `curRate` 状态（`currentProgressRate`）直接驱动 `Video` 组件的播放速率实现即时生效。
	- UI 使用小型圆角按钮显示当前倍速，点击切换并在界面右上角位置显示，便于用户快速切换。

***